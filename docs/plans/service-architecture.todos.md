# Service architecture — judgment calls, ledger, and open questions

Dated bullets, appended by whoever runs a wave of `docs/service-architecture-plan.md`. One line each: what was decided and why. Never reopen a decision here; put a doubt under "Open".

## Decided in the plan (2026-09-18)

- The app runs in its own `ApplicationV2` window with its own `PIXI.Application`. The scene canvas costs a shared-scene requirement, an input contest with Foundry, and a second host for the DOM preview boards.
- Foundry's hex grid API stays unused. `engine/grid.ts` covers both grid kinds plus edges and corners, and the engine stays free of Foundry.
- The persisted record is the only state channel. Persist-first makes a snapshot broadcast redundant.
- The session is one serialized string in a world setting. One active battle per world.
- Saved battles: ten slots in a second world setting, file export and import beyond that. Load is a GM command under the next revision.
- `expectedRevision` plus twenty recent command IDs replaces receipts. Chat delivery records are deferred; event IDs on messages keep that door open.
- The GM draws d20s through `Die#randomFace()` and chat rolls are rebuilt from the recorded faces.
- Undo history stays in the authority's memory, as the prototype's does.
- Five services. Selection and activation end live in `ActionResolutionService`.
- Typed events come from a state comparison plus log tags at free strikes, spell resolution, and secondary dice.
- Default seating: the GM picks a side and every player takes the other. Manual seating is the edge case.
- Turn rotation runs on across rounds and days, so players sharing fewer armies act equally often.
- An offline seat is skipped when its turn opens. A side with nobody online falls to the GM.
- A GM may issue any command for either side; `turn.reassign` hands an open turn to another seat.
- Side-level decisions accept any user seated on that side; the last submission before the side confirms stands.
- Notifications use the app's own host (`src/app/notifications.ts`) on both hosts. Foundry's sits outside the window and takes a bare string.
- Notices are derived per client from adopted records and never enter the session or the socket, so each viewer reads their own part in the turn.
- The one Foundry notification is the turn notice for a player whose window is hidden; a missed turn stalls the table.
- Activity notices expire; every other notice ends by dismissal or by its trigger clearing, as the drag-feedback work decided.
- The session orchestrates with the built-in Workflow tool, one phase per workflow, writing its script from the plan. A saved 280-line orchestrator script was written and removed on 2026-09-18 as more machinery than the job needs.
- The outcome operation ID derives from the battle ID, so a reloaded battle cannot apply its outcome twice.
- ReignMaker owns writes to ReignMaker data through `applyBattleOutcome`; Battlefield reads no ReignMaker flags.

## Judgment calls

- 2026-09-18, Wave 0.1: `assetUrl` computes at each call site rather than caching a module-level `DIR`/`BASE` constant, so a `setAssetBase` call still reaches art requests from modules already imported (`paper.ts`, `ink-sheet.ts`, `terrain-sheet.ts`, `EffectLayer.ts`). `terrain-textures.ts`'s `TEXTURE_CHOICES` stays an eager module-load computation, matching its prior behaviour.
- 2026-09-18, Wave 0.1: exported `setAssetBase` from `src/board/index.ts`'s public barrel, since Foundry's adapter (Wave 0.2) calls it from outside `src/board`.
- 2026-09-18, Wave 0.2: the module id is `battlefield`, so the link in `Data/modules` must carry that name — `assetUrl` resolves art at `modules/battlefield/art/...`. `vite.foundry.config.ts` fails the build if `module.json` and `src/adapters/foundry/module-id.ts` disagree.
- 2026-09-18, Wave 0.2: `app.css` is scoped with `:where(.battlefield-root)`, which adds no specificity, so every selector weighs what it did before and component styles still override it. Foundry's own stylesheet sits entirely in cascade layers and unlayered rules beat every layer, so the module needs no extra weight to hold its own inside the window.
- 2026-09-18, Wave 0.2: the browser's `html, body` rules moved to `src/app/page.css`, which `main.ts` imports and the module build leaves out; its pre-paint background is a literal, since the palette tokens now live on the app root.
- 2026-09-18, Wave 0.2: the app root is `display: contents`, so the browser's layout is untouched and the shell's fixed layers resolve against `.battlefield-content`, which carries `contain: layout`.
- 2026-09-18, Wave 0.2: `withinApp` reads the last pointer press to place a key that lands on `<body>` with nothing focused; containment alone would silence the board's own keys in both hosts.
- 2026-09-18, Wave 0.2: `setAssetBase` runs from `install-asset-base.ts` at load, the first import in the Foundry entry, because `terrain-textures.ts` builds `TEXTURE_CHOICES` the moment it is evaluated. It imports `board/asset-base.js` directly: the barrel would pull in the modules it has to precede.
- 2026-09-18, Wave 0.2: the PIXI shim enumerates the members the app uses rather than re-exporting a namespace, since a runtime global has no static exports; `npm run check` checks that list against pixi.js 7.4.3's own types.
- 2026-09-18, Wave 0.2: `grep -c "PixiJS" dist-foundry/*.js` stands as written — the marker appears 5 times in the browser bundle and 0 in the module bundle, so it separates the two.
- 2026-09-18, Wave 1.1: **reserved, `// proto:`** — the save migration reads the v4 `{ stage, setup, battle }` and derives the lifecycle stage from the battle alone, ignoring the old `stage` string. The old value named a setup tab, which is local state; a v4 save mid-setup therefore reopens at the first unfinished stage rather than the exact tab, until the navigation store of Wave 2.5.
- 2026-09-18, Wave 1.1: **reserved, `// proto:`** — `battleId` is `battle-<base36 time>-<6 random base36>` and `rulesVersion` is the date `2026-09-18`, since the rules document carries no version of its own. Both ride with the ID formats reserved for Wave 2.2.
- 2026-09-18, Wave 1.1: the session key is `battlefield.session.v1` and the record carries `schemaVersion` as well, so the key names the envelope and the field names the shape; `reviveSession` refuses any other `schemaVersion` and the load falls back to `battlefield.v4`, then to a fresh session.
- 2026-09-18, Wave 1.1: the old key is removed inside `load`, after a session revives from the new key — not after `save` — so a write that never comes back intact leaves the v4 save in place.
- 2026-09-18, Wave 1.1: `Setup`, `SetupUnit`, `SetupEngine`, `defaultSetup`, and `randomSeed` moved into `runtime/session.ts` (`Setup` is the review's `BattleSetupDraft`); `game.svelte.ts` re-exports the three types so its consumers keep their imports. `defaultSetup` now states `size: 11`, the value every reader already defaulted to, so a fresh record round-trips unchanged.
- 2026-09-18, Wave 1.1: the repository exposes `loadSessionSync` beside the async port, and `game.svelte.ts` seeds from it. Module-level `$state` is built at import, and a promise would render the default board first; Wave 1.4's read store adopts published sessions and retires this.
- 2026-09-18, Wave 1.1: `// proto:` — a rejected save stays silent, as it was before. Wave 1.4 raises it under the `storage` notice.
- 2026-09-18, Wave 1.1: `BattleEvent` is `{ id, type }` until Wave 3.1 names the ten event types; `lastCommit` carries the shape from the start so the record does not change again.
- 2026-09-18, Wave 1.2: `expectedRevision` rides on every envelope and the executor does not yet enforce it — Wave 3.3 owns that, along with answering a resent command ID. Enforcing it here would refuse the second of two commands issued together, which is this wave's own ordering test.
- 2026-09-18, Wave 1.2: the executor's validation covers the battle ID, a known command type, and a battle under way. Stage, seat, and turn permissions arrive with the policy in Wave 3.3.
- 2026-09-18, Wave 1.2: `recentCommandIds` is recorded at each commit and capped at twenty, so the list Wave 3.3 reads exists from the first command.
- 2026-09-18, Wave 1.2: a rejection resolves as `{ ok: false, reason, message }` rather than throwing, so Wave 1.4 reads the reason to choose between the `command` and `storage` notices, and one lost command cannot break the queue.
- 2026-09-18, Wave 1.2: `ActionResolutionService` takes and returns the whole `BattleSession`, not the `BattleState`. It shares the executor's working record that way, and a later workflow that touches more than `battle` needs no new signature.
- 2026-09-18, Wave 1.2: **reserved, `// proto:`** — a command ID is `cmd-<base36 time>-<6 random base36>`, matching the battle ID of Wave 1.1. It rides with the ID formats reserved for Wave 2.2.
- 2026-09-18, Wave 1.2: `TacticalAction = Action & { unit: string }` in `runtime/commands.ts` holds the requirement at the boundary until Wave 1.3 makes `Acts.unit` required in the engine.
- 2026-09-18, Wave 1.2: `DicePort` is declared in `runtime/ports.ts` with the engine's `Rng` shape rather than re-exporting `Rng`, so Wave 3.1 can wrap it to record faces without touching the engine. `createRuntime` defaults it to `randomRng`.
- 2026-09-18, Wave 1.2: undo history is pushed for `action.resolve` and `activation.end` alone, capped at thirty, which is what the prototype's store did; a selection is not an activation.
- 2026-09-18, Wave 1.2: `createRuntime` takes the loaded session rather than awaiting `repository.load`, since the browser store is seeded before its first render (the Wave 1.1 call); it exposes `submit`, which fills the envelope from the committed record, beside `execute` for a client that builds its own.
- 2026-09-18, Wave 1.3: the measured census undercounted by two — `melee-plan.test.ts:30` and `movement-control.test.ts:49` also omit `unit` — found by an exhaustive grep of `act(` rather than the census table, then confirmed complete by a clean `tsc --noEmit -p tsconfig.engine.json` and `npm run check`.
- 2026-09-18, Wave 1.3: `Battle.svelte`'s `advance` and `charge` literals (lines 632–633) also need `unit`, since `Acts.unit` is required for every action type, not only `move` and `maneuver` as the wave text named; `commit()` and `performManeuver()` now guard `if (!active) return;` before naming `active.id`, matching `performActivity`'s existing guard, instead of asserting a possibly-null unit non-null.
- 2026-09-18, Wave 1.3: `battle.test.ts`'s `refresh` helper resolves its unit via `activeUnit(state)!.id` before calling `act`, since the helper is generic over any battle state and previously relied on the engine's own fallback.
- 2026-09-18, Wave 1.4: `// proto:` — the executor gains `change(edit, history)`, the write path for the setup, lifecycle and continuation functions Phase 2 has yet to turn into commands. Two writers would leave the executor holding a pre-night battle after `resolveNight`, and the next tactical command would act on it and publish day one again; one queue keeps one record and one order. Phase 2 removes it.
- 2026-09-18, Wave 1.4: undo moved onto the executor (`runtime.undo`), which rewinds to the battle its own history kept and commits it under a new revision; the boundaries that cleared the prototype's stack — a committed night, a new day, resolved orders, an answered surrender, a new battle — pass `'clear'` to `change`.
- 2026-09-18, Wave 1.4: a direct change draws a command ID and fills `lastCommit` and `recentCommandIds` like a command, so the record carries one commit shape whichever path wrote it.
- 2026-09-18, Wave 1.4: the read store adopts `battle` and `history` from each published record and leaves `setup` alone. The setup panels edit `game.setup` continuously, and replacing that object on every save would drop an edit made between the write and the publish; `game.setup` is seeded with a `structuredClone` of the record's, so an unsaved edit never reaches the executor's copy.
- 2026-09-18, Wave 1.4: `game.stage` stays the local setup tab and every write derives the record's lifecycle stage from the battle, as Wave 1.1 decided. `startBattle` moves the local stage only after its commit succeeds; `backToSetup` moves first, as it did.
- 2026-09-18, Wave 1.4: `endActivation()` names the active unit from `game.battle.active`, since `activation.end` carries a unit ID. With no active unit the store refuses locally and sends nothing, in place of the old silent return.
- 2026-09-18, Wave 1.4: `deselectUnit` still commits when the engine returns the same state, so a stray click on bare ground costs a revision and a write — the prototype saved on that click too.
- 2026-09-18, Wave 1.4: `app/command-notices.ts` holds the `command` and `storage` IDs and one `commandReporter(notifications)` for both views. A success dismisses both notices, so a failed save stands until the next save gets through. The wording carries over from the notices it replaces: "Action unavailable" from `Battle.svelte`'s `commit` and "Changes could not be saved" from `TextureLab.svelte`.
- 2026-09-18, Wave 2.1: `runtime/commands.ts` declares `PaintBrush`/`PaintStroke` fresh, structurally mirroring `board/brush.ts`'s `Brush`, so `runtime` names no type from `src/board` — the command boundary carries plain data.
- 2026-09-18, Wave 2.1: the executor gates on `SETUP_COMMANDS` — a setup command is refused with `stage` while a battle is under way, and a tactical command is refused with `stage` while none is; map preparation and the battle in progress are mutually exclusive stages, matching that setup panels and `Battle.svelte` are never shown together.
- 2026-09-18, Wave 2.1: undo history becomes `HistorySnapshot = { kind: 'battle'; battle } | { kind: 'setup'; setup }`, replacing the old `BattleState[]`. Only `setup.paint` is undoable among the setup commands — `generate`, `rerollSeed`, `editSpec`, and `setRoundsPerDay` are not — matching what the prototype's local `Paint.svelte` stack ever restored.
- 2026-09-18, Wave 2.1: a paint stroke's placement cleanup (`clearWaterPlacements`) checks only whether a square is now water, not the rank/ambush rules `canDeploy` applies for `generate`/`rerollSeed`'s `clearUndeployable` — a stroke can only newly forbid water, never move a deploy rank, so the cheaper check is deliberate, not a gap against `canDeploy`.
- 2026-09-18, Wave 2.1: `BoardSetup.svelte`'s grid and size selects submit two sequential commands (`editSpec` then `generate`, in `editAndRegenerate`) rather than one atomic one; the second runs only if the first is accepted. A rejection of the second leaves the spec edited but the board unregenerated, which the command notice surfaces.
- 2026-09-18, Wave 2.1: `game.svelte.ts` adds `syncSetup()`, replacing the panel's local `game.setup` from the committed record after every setup command and after an undo — unlike `save()`, which still carries the panel's own edit forward for the fields Phase 2 has yet to turn into commands. The service computes the next `setup` on the executor's own copy, so the panel must read it back rather than assume its optimistic edit matches.

- 2026-09-18, Wave 2.2: **reserved, `// proto:`** — a unit ID is `unit-<base36 time>-<6 random base36>` and an equipment ID `eq-<...>`, minted by `mintId(prefix)` in `runtime/session.ts` beside the battle ID of Wave 1.1 and the command ID of Wave 1.2. One shape for every identity the record holds.
- 2026-09-18, Wave 2.2: `Deployment.id`, `Emplacement.id`, and an attached engine's `id` are optional in the engine, and `createBattle` falls back to `positionalUnitId`/`positionalAttachedId`/`positionalEmplacedId` — the `u0…` scheme every battle used before this wave. The engine suite builds 88 deployments from literals and asserts `'u0'` 759 times; requiring IDs there would rewrite the rules baseline for nothing. The Done grep passes on its literal terms, and the positional scheme survives as a named fallback rather than as inline index arithmetic.
- 2026-09-18, Wave 2.2: engines inside a migrated battle take fresh equipment IDs (`repairBattleIds`), units keep theirs. Nothing outside the record ever named an engine — the board built `engine:${i}` at render — so there is no old identity to preserve, and `previousBattlefields` is repaired with the live engines so an archived field stays typed.
- 2026-09-18, Wave 2.2: a unit's attached engines become `{ id, name }` in the setup draft and `AttachedEngine { card, id? }` at `createBattle`'s boundary, in place of a bare name. Two engine tests pass `{ card }` now; no expected result moved.
- 2026-09-18, Wave 2.2: `Place.svelte`'s `Pick` is `{ kind, id }`, tokens carry the piece's own ID, and `pickOf` reads the kind from which list holds the ID. `idOf` goes — a token ID no longer encodes a position. Wave 2.3 moves these mutations behind `ArmyPreparationService`; the selection contract is already by ID for it.
- 2026-09-18, Wave 2.2: the Svelte MCP `svelte-autofixer` was unavailable in this session, as in Wave 2.1; `npm run check`'s svelte-check pass over `Place.svelte` and `Battle.svelte` stood in for it.

- 2026-09-18, Wave 2.3: `army.addUnit` carries the whole `UnitCard` rather than a library name, so a campaign card imported in Phase 5 needs no library entry to enter a force. `army.addEmplacement` and `army.attachEquipment` carry an engine name instead, since `ENGINES` is the one closed list and the service validates the name against it.
- 2026-09-18, Wave 2.3: the service mints the new piece's ID, so `Place.svelte` selects the piece the command appended — `mine.at(-1)` after the commit — rather than naming an ID it minted itself. A client that minted IDs would decide identity before the authority accepted the command.
- 2026-09-18, Wave 2.3: the deployment queries (`deployableCells`, `cellsFor`, `autoCell`, `sideReady`, `isAmbush`, `pieceOf`) are exported from the service as pure functions over a `BattleSetupDraft` and called straight from the view for the deploy wash and the Place button's enabled state. A query needs no commit, and the view's own pre-check keeps an invalid drop a silent no-op instead of a refused command.
- 2026-09-18, Wave 2.3: no `army.*` command is undoable, matching Wave 2.1 — the prototype's only setup undo was a paint stroke. All ten are in `SETUP_COMMANDS`, so the executor refuses them once a battle is under way.
- 2026-09-18, Wave 2.3: `army.generateForce` takes an optional seed on the envelope and defaults to `randomSeed()`; the store sends none and the tests send one, so a generated force is reproducible from the command when it matters.
- 2026-09-18, Wave 2.3: `syncSetup()` now assigns the record's fields onto the panel's `game.setup` and replaces `board` only when the record's own board object changed. Replacing the whole draft gave the board a fresh identity on every placement, and `PixiBoard`'s `setBoard` effect redraws the terrain on identity alone.
- 2026-09-18, Wave 2.3: the store normalizes a `PieceRef` and a `UnitCard` to plain data before submitting (`plain`, `$state.snapshot`), since a Svelte proxy reaching `structuredClone` inside the service would throw.
- 2026-09-18, Wave 2.3: the Svelte MCP `svelte-autofixer` was unavailable in this session, as in Waves 2.1 and 2.2; `npm run check`'s svelte-check pass over `Place.svelte` stood in for it.

- 2026-09-18, Wave 2.4: the session gains `nightDeclarations` beside the `nextDeployment` the wave named. A per-side recovery submission needs the same holding place a per-side deployment does, so both are `SideSubmissions<T>` on the record; Wave 3.5 folds the pair into `InteractionRecord` without changing the data.
- 2026-09-18, Wave 2.4: **reserved, `// proto:`** — no schema bump for the two new fields. `reviveSession` fills each with `{}` in place, as Wave 2.2's ID repair did, so a `battlefield.v4` save and every session written since still load. It rides with the migration shape reserved from Wave 1.1.
- 2026-09-18, Wave 2.4: `continuation.chooseBattlefield` carries a `Partial<BoardSpec> | null` rather than a generated `Board`, so the authority draws tomorrow's map and `BattleReport.svelte` no longer calls `generateBoard`. The service merges the partial over the field already chosen — today's grid and size, today's fortification dropped, a fresh seed for the first new field — which is what the view computed before.
- 2026-09-18, Wave 2.4: the executor's `UNDOABLE` boolean becomes a three-way `HISTORY` table (`push`, `keep`, `clear`), since a command can now be one of the boundaries the prototype's store cleared at. `continuation.declareRecovery` clears at the first army's declaration rather than at the night: with one writer there is no other moment, and an undo across a declaration would rewind a battle the other army has already answered.
- 2026-09-18, Wave 2.4: `declareRecovery` refuses a choice naming a unit on the other side, so one seat cannot declare for both armies; the engine still rules on whether that unit may recover at all. The two declarations reach `recoverAtNight` in `SIDES` order, so the dice fall the same way whichever army declared first.
- 2026-09-18, Wave 2.4: `declareDeployment` validates every cell it is handed but accepts an incomplete submission; `startNextDay` demands a cell for every standing unit on both sides. A view builds a side's placement before it submits, and the command that starts the day is the one that needs the whole field.
- 2026-09-18, Wave 2.4: `deploymentProblem(field, side, positions, complete)` lives in `ArmyPreparationService` and returns a reason or null, per the plan's "`ArmyPreparationService` supplies deployment validation for later days"; `BattleContinuationService` turns the reason into the throw the executor reports as a refusal.
- 2026-09-18, Wave 2.4: `chooseBattlefield` clears `nextDeployment` on every choice, a return to the same ground included, because the cells were chosen against a field the choice may have replaced. `startNextDay` clears both submissions as it consumes them, and `backToSetup`/`resetSetup` drop them with the battle.
- 2026-09-18, Wave 2.4: **reserved, `// proto:`** — the per-side wording reads "Commit attacker recovery" / "The attacker has declared" and "Submit deployment" / "Deployment submitted", with "Both armies declare, then every selected unit rolls together" under the recovery modifiers. It joins the open player-facing wording item.
- 2026-09-18, Wave 2.4: the Svelte MCP `svelte-autofixer` was unavailable in this session, as in Waves 2.1–2.3; `npm run check`'s svelte-check pass over `BattleReport.svelte` stood in for it.

- 2026-09-18, Wave 2.5: the executor's proto `change` path and its separate `undo()` method are gone; `session.undo` is a command in the union, routed to the rewind before the service table, and `Executor.execute` is the only way into the record. Invariant 5 holds from here: `src/app` reaches the record through `runtime.submit` alone.
- 2026-09-18, Wave 2.5: `SETUP_COMMANDS` becomes `COMMAND_STAGE`, a `setup | battle | any` entry for every command, because `session.undo` crosses the line a set of setup commands drew. The same table is the executor's known-command guard for the socket payloads of Wave 4.2.
- 2026-09-18, Wave 2.5: `battle.finalize` refuses a battle that is still being fought or that `canContinueBattle` says can go on, and a session at stage `finalized` refuses every command but `battle.returnToSetup` — the outcome has been reported to the campaign, so undo and further play would rewrite a result already handed over. Wave 5.5 attaches its writeback to this transition.
- 2026-09-18, Wave 2.5: `BattleManager` takes no injected dependencies. `createBattle`'s rng is unused (`_rng`) and the next-day transition draws no dice, so the manager composes pure engine calls and the pure exports of the services it spans (`applyStroke`, `clearWaterPlacements`, `deploymentProblem`, `sideReady`) rather than holding service instances.
- 2026-09-18, Wave 2.5: `clearWaterPlacements` moves to `ArmyPreparationService` and `applyStroke` is exported from `MapPreparationService`; `setup.paint` routes to `BattleManager.paint`, which joins them. `generate` and `rerollSeed` keep their own `clearUndeployable` inside the map service — the plan names painting alone as the combined change, and a generated board replaces the whole draft anyway.
- 2026-09-18, Wave 2.5: `battle.start` refuses unless `sideReady` holds for both sides, so the manager, not the view's disabled button, is what keeps a half-deployed force off the field. The engine list lookup still drops an unknown engine name silently, as it did in `startBattle`.
- 2026-09-18, Wave 2.5: all three stage transitions move the local tab only after the commit is accepted, where Wave 1.4 let `backToSetup` move first. A refused `battle.returnToSetup` used to leave the player on the setup panels over a live battle, with the next setup command refused.
- 2026-09-18, Wave 2.5: `game.setup` stays a `structuredClone` of the committed draft rather than the record's own object, now adopted inside `runtime.subscribe` in place of Wave 2.1's `syncSetup()` calls. No view writes it any more, so the clone guards only against an accidental reach into the executor's record; the board identity check of Wave 2.3 rides along unchanged.
- 2026-09-18, Wave 2.5: `forward()` returns `go: () => Promise<CommandResult> | null` — null for the steps that only turn the page — so `StageNav` can await the one step that submits a command and report a refusal, closing the Wave 1.4 reviewer note about a silent storage failure at "Begin the battle".
- 2026-09-18, Wave 2.5: **reserved, `// proto:`** — the transitions' refusal messages, which the `command` notice shows verbatim: "the attacker has a piece still off the board", "this battle is finalized", "the battle is still being fought", and "the day ended at dusk and the battle can go on". They join the open player-facing wording item.
- 2026-09-18, Wave 2.5: the Svelte MCP `svelte-autofixer` was unavailable in this session, as in Waves 2.1–2.4; `npm run check`'s svelte-check pass over the six touched components stood in for it.

- 2026-09-18, Wave 2.6: `BattleArchive.load` and `.export` hand back whatever schema a slot was written in (`unknown` and a JSON string) rather than an already-revived `BattleSession`; the new `session.load` command is what migrates, matching how `SessionRepository.load` already migrates `battlefield.v4` outside the port. `session.ts` gains `migrateSession(value)`, trying `reviveSession` then falling back to `migrateLegacySave` — the same two functions `loadSessionSync` already calls, named together for the archive's benefit.
- 2026-09-18, Wave 2.6: `save`, `list`, `remove`, `export`, and `import` run straight from `SaveLoadPanel.svelte` against the archive port, with no command — the review's own words for this wave say saving "changes no shared state and needs no command." Invariant 5 ("views call no persistence function") is read as scoped to the one shared session record `executeCommand` owns; the archive is a side store of inert slots, and `session.load` is the one archive operation that touches shared state, so it alone is a command. Flagged for the reviewer to confirm this reading holds.
- 2026-09-18, Wave 2.6: `session.load` is handled in `executeCommand.ts` beside `session.undo`, but its edit closure runs `migrateSession` and reshapes the result before handing it to the same `persist()` every other command uses — so a bad or missing slot rejects through the ordinary `engine`/`storage` paths, and the authority still saves the migrated record before it acknowledges, same as any other command.
- 2026-09-18, Wave 2.6: the finalized-stage guard in `executeCommand.ts` now exempts `session.load` alongside `battle.returnToSetup` — loading a different save replaces the finalized battle outright rather than rewriting its outcome, so it doesn't need to leave the record first.
- 2026-09-18, Wave 2.6: **reserved, `// proto:`** — an archive slot ID is `slot-<base36 time>-<6 random base36>`, minted in `src/adapters/browser/localArchive.ts` and never seen outside the browser store. It joins the open Stable ID format item.
- 2026-09-18, Wave 2.6: the browser archive is one `battlefield.archive.v1` localStorage key holding a JSON array of `{ slot, name, savedAt, day, round, data }`; no slot cap. The review's ten-slot cap is stated for the Foundry world setting a later wave adds, not for this browser store.
- 2026-09-18, Wave 2.6: the save/load panel lives inside `src/app/shell/TopBar.svelte` itself, not behind a `tools` snippet in each of the four stage views, so every stage gets it from one edit; `BoardSetup.svelte`, `Paint.svelte`, `Place.svelte`, and `Battle.svelte` are unchanged.
- 2026-09-18, Wave 2.6: the Svelte MCP `svelte-autofixer` was unavailable in this session, as in every prior wave that touched `.svelte` files; `npm run check`'s svelte-check pass over `TopBar.svelte` and the new `SaveLoadPanel.svelte` stood in for it.

- 2026-09-18, Wave 3.1: **reserved, `// proto:`** — the ten event types are exactly the plan's list, in `src/runtime/events.ts`, and the three log tag kinds are `freeStrike`, `spell`, and `secondDie` in `src/engine/types.ts`. Both carry `// proto:` and join the open event-list item.
- 2026-09-18, Wave 3.1: the `spell` tag carries its own `caster` and the `freeStrike` tag its own `attacker`, rather than reading the actor off `LogEntry.unit` — Controlling announces itself through the target's save, so its line is attributed to the target while the event names the caster.
- 2026-09-18, Wave 3.1: Controlling's resist line is tagged as the seventh spell site, since that tree writes no "casts" line of its own; an entry with a `spell` or `freeStrike` tag yields that event and no `checkResolved`, so a free strike counts once.
- 2026-09-18, Wave 3.1: the `secondDie` tag sits at the `const second =` draw alone, as the wave names, and yields no event of its own — the event list holds none. `attackRoll`'s sure-strike/ward pair keeps its untagged prose; the faces of both draws reach `lastCommit.dice` either way.
- 2026-09-18, Wave 3.1: events are grouped by kind — movement, then the log's checks and tags, then wounds/disorder/routs, then activation, round and battle ends — rather than interleaved in the engine's own order. An advance therefore yields its move before its melee events, as the wave requires; a maneuver's move also precedes the check that failed it, which Wave 3.2's presentation orders as it likes.
- 2026-09-18, Wave 3.1: `// proto:` — only `action.resolve` and `activation.end` carry events. A setup, army, continuation or lifecycle command replaces whole boards and forces, which a client adopts rather than plays; the derivation is ready for them when a wave wants them.
- 2026-09-18, Wave 3.1: a route falls back to `[from, to]` when `movePath` on the prior state finds none — a unit in contact, one shoved by an Overrun, or one set down by Translocate has no stride route. An advance's legs come from its `MeleePlan`, and a final leg is added when the unit ends off the plan's path, which is what an Overrun's taken ground does.
- 2026-09-18, Wave 3.1: `CommitRecord.dice` is required, not optional; `reviveSession` fills it (and `events`) on a record written before this wave, and one test fixture in `session-repository.test.ts` gains `dice: []`. No assertion value moved.
- 2026-09-18, Wave 3.1: the dice recorder wraps the port once in `createRuntime` and every service rolls through it, so the executor takes the faces of a transition without knowing which service drew them. `persist` drops the faces of a rejected edit before the next command rolls.

- 2026-09-18, Wave 3.2: `createPresentation(seed)` is built in `game.svelte.ts` and observes each record inside `runtime.subscribe`, before the store adopts it. A token spends its route on the move its new position triggers, so `setRoute` has to reach the board ahead of `setTokens`; the store's assignment schedules that effect.
- 2026-09-18, Wave 3.2: `Battle.svelte`'s `commit()` no longer calls `setRoute` with the path the drag traced — the route comes from the `unitMoved` events instead, which is the only route a client that did not drag can have. An advance's two legs are joined into one road, since the token spends one route per move and a second `setRoute` would replace the first.
- 2026-09-18, Wave 3.2: **`// proto:`** — `checkResolved` names no target, so the resolved markers and arrows of an ordinary attack or a rally are read from the pieces the acting unit (`previous.battle.active`) wounded or disordered in the same commit; a cast and a free strike name their own targets. A blow that misses now leaves no mark, where the pre-command resolution drew one. A commit that ends the round marks nothing: the round's end settles persistent wounds across the field that the closing unit did not deal.
- 2026-09-18, Wave 3.2: a blast that catches nobody plays no burst, since `spellResolved.targets` holds the units caught rather than the hexes of the shape; the same rule leaves Guard and Steady without the self-marker the local resolution drew. A charge or an advance now gains the afterglow it never had, because its wounds are events like any other.
- 2026-09-18, Wave 3.2: `TargetingService.resolve` keeps `markers`, `arrows`, and `effects`. `Battle.svelte` reads only `action` now; the other three stay as the class's own specification and are asserted in `targeting.test.ts`.
- 2026-09-18, Wave 3.2: `src/tests/presentation.test.ts` tests the pure `commitPlay(previous, next)` — the wave's named contract is a revision comparison, which needs no component. The sink that drives PIXI stays under prototype mode, untested.
- 2026-09-18, Wave 3.2: the Svelte MCP `svelte-autofixer` was unavailable in this session, as in Waves 2.1–2.6; `npm run check`'s svelte-check pass over `Battle.svelte` stood in for it.

## Open, for Mark

- **Save migration shape** (Wave 1.1). The v4 stage is dropped, the lifecycle stage comes from the battle, `battleId` is `battle-<base36 time>-<random>`, and `rulesVersion` is a date. **Decision:**
- **Stable ID format** (Wave 2.2, extended Wave 2.6). Units are `unit-<base36 time>-<6 random base36>`, equipment `eq-<...>`, matching the battle ID of Wave 1.1 and the command ID of Wave 1.2; Wave 2.6 adds archive slots as `slot-<...>` in the same shape. A deployment that names no ID still falls back to `u0…`, which keeps the engine suite and every save written before this wave. **Decision:**
- **Event types and log tag names** (Wave 3.1). The ten types are the plan's list, in `src/runtime/events.ts`, each stamped `${commandId}:${index}`. The log tags are `freeStrike`, `spell`, and `secondDie`, set at nine sites: the free strike in `resolveStrike`, the seven casts (Controlling's on the target's save), and the second die a Blast throws. Wave 3.2 adds one question to this: `checkResolved` names no target, so the board infers an attack's mark from the pieces the acting unit changed and a miss leaves none. Say whether `checkResolved` should carry its target. **Decision:**
- **Player-facing wording for turns, seats, and notices** (Waves 3.5, 3.6, 4.4). Wave 1.4 keeps "Action unavailable" for a refused command and "Changes could not be saved" for a failed write, both carried over from the notices they replace. Wave 2.4 adds the per-side recovery and deployment lines; Wave 2.5 adds the lifecycle refusals ("the attacker has a piece still off the board", "this battle is finalized"). **Decision:**
- **Window title and scene-control tooltip** (Wave 0.2 drafted "Battlefield"; outside the reserved list, offered at gate 0). **Decision:**

## Ledger

One line per wave: date, wave, model, commits, gate, verdict, escalations with their cause.

- 2026-09-18, Wave 0.1, Sonnet, 0b6b3c0, gate pass, APPROVE, no escalation. Reviewer note for 0.2: `TEXTURE_CHOICES` bakes its URLs at module evaluation, so `setAssetBase` must run before the barrel loads.
- 2026-09-18, Wave 0.2, Opus, 61b86f9 a4404f6 81bb941 + a674697 (svelte autofixer), gate pass with `build:foundry`, APPROVE, no escalation. Reviewer notes carried forward: `withinApp` returns true with no root, so the ctrl-wheel listener installed at `init` cancels page-wide before the window opens (masked by Foundry's own handler); two quick `open()` calls can mount the shell twice before `rendered` flips; `VfxGallery.svelte`'s `svelte:window` keydown is ungated; `_preClose` unmounts before the close animation, so the window shrinks empty.
- 2026-09-18, Wave 1.4, opus, 2379de7 08df9c8 11277ac bff200b b81eaa752a29688f2e5e75b537d9b6c22f8a244b, gate pass, APPROVE, no escalation. approved. Reserved: Player-facing wording for notices: the refusal reads "Action unavailable" and the failed write reads "Changes could not be saved". Both carry over from the notices they replace (Battle.svelte's `commit` and TextureLab.svelte), marked provisional and appended to the open "Player-facing wording" item for Mark.; Player-facing notice wording: 'Action unavailable' for a refused command and 'Changes could not be saved' for a failed write (flagged under Open, for Mark at docs/plans/service-architecture.todos.md:77); Command ID format `cmd-<base36 time>-<random>` now also stamps direct changes and undo commits via `newCommandId()` in executeCommand.ts:123,142 (already under the Stable ID format item at todos.md:75). Reviewer notes: src/app/game.svelte.ts:101,106,116 and callers in Place.svelte, Paint.svelte, BoardSetup.svelte, StageNav.svelte:25 — `next`, `back`, `goToStage` call `void save()` and the setup panels call `save()` bare, so a failed write during setup still shows nothing; `startBattle` through `forward().go` also discards its result, so a storage failure at 'Begin the battle' leaves the user on the deployment tab with no notice; src/app/Battle.svelte:638 — In `commit()` the old `try` skipped `meleeTarget = null; meleeSelected = null;` when `takeAction` threw; the awaited form now clears both after a refused command too; src/app/game.svelte.ts:176-180 — `chooseNextBattlefield` used to return silently outside a dusk-ended day; it now throws inside the edit and surfaces 'Action unavailable'; src/runtime/executeCommand.ts:117 — `history.slice(1 - HISTORY_LIMIT)` plus the push keeps 30 entries where the prototype's `slice(-30)` plus push kept 31.
- 2026-09-18, Phase 0 stopped at Human gate 0. Link `dist-foundry` into `Data/modules/battlefield` (the id `assetUrl` depends on).
- 2026-09-18, Mark's instruction: run Phases 1–5 overnight without stopping. Human gates 0–5 are deferred to one play session after the run; reserved calls stay open with `// proto:` choices in the code; the `CLAUDE.md` amendment for Phase 1 was made by the session; Wave 5.3 (ReignMaker) is skipped and Wave 5.5 reaches `applyBattleOutcome` through a port so ReignMaker can attach later.
- 2026-09-18, Wave 1.2, opus, 9441d4f 754379b, gate pass, APPROVE, no escalation. approved. Reserved: Stable ID format: a command ID is `cmd-<base36 time>-<6 random base36>`, matching Wave 1.1's battle ID. Marked `// proto:` in src/runtime/commands.ts, recorded in the todos, and appended to the open 'Stable ID format' question for Wave 2.2.; Command ID format `cmd-<base36 time>-<6 random base36>` (src/runtime/commands.ts:38, `// proto:`), folded into the open Stable ID format item for Wave 2.2 in docs/plans/service-architecture.todos.md. Reviewer notes: src/runtime/executeCommand.ts:88-89 — Listeners run inside `run` with no try around them; docs/plans/service-architecture.todos.md:58 — The todos bullet says the cap of thirty is what the prototype's store did; docs/plans/service-architecture.todos.md:50-51 — `expectedRevision` rides on every envelope but is neither compared nor used to answer a resent command ID from `recentCommandIds`; docs/plans/service-architecture.todos.md:55,65 — The command ID format `cmd-<base36 time>-<6 random base36>` is marked `// proto:` at `src/runtime/commands.ts:38`, recorded as a dated reserved bullet, and appended to the open Stable ID item for Mark.
- 2026-09-18, Wave 2.1, sonnet, c963ae0, gate pass (`npx vitest run`, `npm run check`, `npx vite build`, `npm run build:foundry`), no escalation. Resumed from an uncommitted `MapPreparationService.ts` and matching diffs already present in the tree at the start of this run — evidence of a prior interrupted attempt at this exact wave; reviewed the diff against the wave text and the invariants rather than discarding it, per Mark's standing instruction to run overnight without stopping. Fixed one broken assertion in `executor.test.ts` against the new `HistorySnapshot` shape and added `src/tests/map-preparation.test.ts` for the wave's two named tests. No new reserved-for-review item; the ID-format and wording items already open carry forward unchanged. The svelte MCP autofixer tool named in the wave procedure was not available in this session; `npm run check`'s svelte-check pass on both touched `.svelte` files stood in for it.
- 2026-09-18, Wave 2.2, opus, 473703b 3dd0813, gate pass, APPROVE, no escalation. approved. Reserved: Stable ID format: `unit-<base36 time>-<6 random base36>` for units and `eq-<...>` for equipment, marked // proto: at src/runtime/session.ts (mintId), recorded in docs/plans/service-architecture.todos.md, and folded into the open 'Stable ID format' item for Mark.; Save migration shape (already open from Wave 1.1, extended here): a migrated setup gets fresh piece IDs, a migrated battle keeps its u0… unit IDs, and its engines take fresh equipment IDs.; Stable ID format: `unit-<base36 time>-<6 random base36>` and `eq-<...>` via `mintId(prefix)` at src/runtime/session.ts:45-52, marked `// proto:`; open decision at docs/plans/service-architecture.todos.md:88.; Optional IDs at the engine boundary: `createBattle` keeps the positional `u0…` / `engine:<n>` / `<unit>:engine:<n>` scheme as a named fallback for deployments without IDs (src/engine/battle.ts:33-38); todos.md:79. Mark decides whether the engine should require IDs and the engine suite be rewritten.; Save migration shape: no schema bump; in-place repair of setup IDs and fresh `eq-` IDs for engines in a running battle, including `previousBattlefields` (src/runtime/session.ts:118-144); todos.md:80 and the open Wave 1.1 item at todos.md:87.. Reviewer notes: src/engine/battle.ts:33-38 — `Deployment.id`, `Emplacement.id`, and `AttachedEngine.id` are optional, and `createBattle` falls back to `positionalUnitId`/`positionalAttachedId`/`positionalEmplacedId`, which reproduce the `u0…`, `${unitId}:engine:${slot}`, and `engine:${index}` scheme; src/runtime/session.ts:45-52 — Unit IDs are `unit-<base36 time>-<6 random base36>` and equipment IDs `eq-<...>`, minted by `mintId(prefix)`, marked `// proto:`, recorded as a dated bullet at todos.md:78, and carried into the open decision at todos.md:88; src/runtime/session.ts:118-144 — `SCHEMA_VERSION` is not bumped; `repairSetup` fills missing unit, attached-engine, and emplacement IDs in place and accepts the pre-2.2 `engines: string[]` shape, and `repairBattleIds` gives fresh `eq-` IDs to engines in a running battle (including `previousBattlefields`) while leaving unit IDs untouched; src/tests/stable-ids.test.ts:44-55 and :63-83 — Both named cases exist and assert the named behavior: removing `units[0]` leaves the remaining unit IDs and emplacement IDs equal to the pre-removal battle's (compared through `createBattle`), and a legacy `{ stage, setup, battle }` save migrated through `migrateLegacySave` keeps `['u0', 'u1']` while its engines gain `eq-` IDs.
- 2026-09-18, Wave 2.3, opus, fc6af13 b7c19e8 525cae7, gate pass, APPROVE, no escalation. approved. Reserved: Stable ID format (already open from Wave 2.2, unchanged here): the service mints unit and equipment IDs through mintId in src/runtime/session.ts, still marked // proto:. This wave adds no new ID shape and answers no new reserved question.; No new reserved item this wave. The service mints unit and equipment IDs through Wave 2.2's `newUnitId`/`newEquipmentId`, whose format remains the open item recorded under 'Open, for Mark' in docs/plans/service-architecture.todos.md.; Judgment calls recorded as eight dated 2026-09-18 Wave 2.3 bullets in docs/plans/service-architecture.todos.md: command payload shapes (whole UnitCard vs engine name), authority-minted IDs with `mine.at(-1)` reselection, pure query exports called from the view, no `army.*` command undoable, optional seed on `army.generateForce`, field-wise `syncSetup` with board identity check, plain-data normalization before submit, and svelte-autofixer unavailability.. Reviewer notes: src/app/game.svelte.ts:66-85 — `syncedBoard` tracks the record's board object, but `save()` (called by `next`, `back`, `goToStage`), `startBattle`, and `resetSetup` replace the record's `setup` with a `$state.snapshot` without updating `syncedBoard`; src/app/Place.svelte:148-154 — `onTrayDrop` pre-checks against `highlight`, which follows `selected`; `onTrayDragStart` sets `selected = p` so the two agree for a tray drag; src/app/Place.svelte:76-79 — `drop` clears `selected` before the remove command resolves; a rejected removal leaves nothing selected while the piece stays in the tray; docs/service-architecture-plan.md — No `Arch ledger: Wave 2.3` commit exists yet; Wave 2.2's ledger commit followed its review, so this is expected at this point rather than a gap..
- 2026-09-18, Wave 2.5, opus, fc88ed5 c5baa30 c2a6994 3bbdb77, gate pass, APPROVE, no escalation. approved. Reserved: Player-facing wording (open item): the new transition refusals read 'the attacker has a piece still off the board', 'this battle is finalized', 'the battle is still being fought', and 'the day ended at dusk and the battle can go on'. Provisional, recorded as a reserved `// proto:` bullet and appended to the open wording item at docs/plans/service-architecture.todos.md.; Player-facing wording: the four lifecycle refusal messages in src/services/BattleManager.ts — 'the <side> has a piece still off the board' (line 42), 'generate a board first' (line 40), 'the battle is still being fought' and 'the day ended at dusk and the battle can go on' (lines 88-89) — plus 'this battle is finalized' in src/runtime/executeCommand.ts:182. Recorded in docs/plans/service-architecture.todos.md under Wave 2.5 and appended to the open 'Player-facing wording' item; no `// proto:` marker in code.; Lifecycle policy touching play: a session at stage `finalized` refuses every command except `battle.returnToSetup`, including `session.undo` (src/runtime/executeCommand.ts:181-183). The executor chose this so a reported outcome cannot be rewritten; Wave 5.5 attaches writeback here. Mark confirms this is the intended relationship between finalization and undo.; Undo boundary already in the table from Wave 2.4: `continuation.declareRecovery` clears history on the first side's declaration rather than when the night commits (src/runtime/executeCommand.ts:39). Carried forward unchanged; flagged for the rule check.. Reviewer notes: src/tests/battle-manager.test.ts:107 — `ended` is a fresh runtime with empty history, so `session.undo` rejects with `reason: 'stage'` from `rewind`'s 'nothing to undo' branch even if the `session.stage === 'finalized'` guard were deleted; src/services/BattleManager.ts:42-45,87-89 — The todos bullet records the four lifecycle refusal messages as `**reserved, // proto:**`, but the diff adds no `// proto:` comment at the message sites in `BattleManager.ts`; src/services/BattleManager.ts:41 — `startBattle` previously answered a missing board with the store's own `refuse(...)` (`reason: 'stage'`); `battleFrom` now throws inside the edit, so the result carries `reason: 'engine'` with the same message; src/app/Place.svelte:375 — `onclick={() => void resetToExample()}` discards the result, so a refused `battle.reset` shows no notice.
- 2026-09-18, Wave 2.6, sonnet, fa93260 dfc4b5ab7b06beb677825d9f5af1ae943d209965, gate pass, APPROVE, no escalation. approved. Reserved: Stable ID format (Wave 2.2, extended here): archive slot IDs are slot-<base36 time>-<6 random base36>, // proto: in src/adapters/browser/localArchive.ts, joined to the open 'Stable ID format' item at docs/plans/service-architecture.todos.md for Mark to rule on all ID formats together.; Archive slot ID format: slot-<base36 time>-<6 random base36>, minted in src/adapters/browser/localArchive.ts:9-10 (joins the open Stable ID format item); Reading of Invariant 5 for archive writes: list/save/remove/export/import run from SaveLoadPanel against the port with no command; session.load alone is a command (todos bullet, Wave 2.6); Save migration shape for archived slots: migrateSession in src/runtime/session.ts:220 tries reviveSession then migrateLegacySave, the same path battlefield.v4 takes; a legacy slot mints a fresh battleId on each load. Reviewer notes: src/app/SaveLoadPanel.svelte:2 — SaveLoadPanel calls archive.list/save/remove/export/import through game.svelte.ts without a command; only session.load is a command; src/runtime/executeCommand.ts:147 and :220 — The edit clears recentCommandIds, then commit() appends the load command's own ID, so the committed record holds [loadCommandId]; src/runtime/session.ts:217-222 — migrateSession composes reviveSession and migrateLegacySave for the archive path; src/adapters/browser/localArchive.ts:12-20 and :22-23 — readAll returns [] on unparseable archive JSON, and the next save() then overwrites the store with a single entry, discarding unreadable slots silently.
- 2026-09-18, Wave 3.1, opus, ecb7cf9 32b0994 dceb82d, gate pass, APPROVE, no escalation. approved. Reserved: Event types and log tag names (reserved): the ten types are exactly the plan's list in src/runtime/events.ts, each stamped `${commandId}:${index}`; the tag kinds are `freeStrike`, `spell`, and `secondDie` in src/engine/types.ts. Both sites carry `// proto:`, recorded under Wave 3.1 in docs/plans/service-architecture.todos.md and folded into the open 'Event types and log tag names' item for Mark.; Stable ID format (already open, extended): an event ID is `${commandId}:${index}`, which inherits the `cmd-<base36 time>-<6 random base36>` command ID of Wave 1.2. No new shape; it rides with the open Stable ID item.; Event type list: the plan's ten types verbatim in src/runtime/events.ts, each stamped `${commandId}:${index}`; only action.resolve and activation.end carry events (// proto:); Log tag names: freeStrike (attacker, target), spell (caster, tree, activity, targets), secondDie (faces) in src/engine/types.ts; Controlling tagged on the target's resist line; an entry with a spell or freeStrike tag yields no checkResolved; Event ordering: grouped movement, then log checks/tags, then wounds/disorder/routs, then activation/round/battle ends, rather than the engine's interleaved order; Route fallback: [from, to] when movePath on the prior state finds none; advance legs from the matching MeleePlan plus a final leg for Overrun ground; CommitRecord.dice required; reviveSession fills events and dice on records written before this wave (migration shape, already reserved). Reviewer notes: src/services/ActionResolutionService.ts:35-39 — routeOf falls back to [from, to] whenever movePath finds no stride route; src/services/ActionResolutionService.ts:97-100 — unitRouted fires only when isRouted flips true, and isRouted requires status === 'active'; src/engine/battle.ts:1334-1338 — The secondDie tag sits on the Blast 'thrown twice' line alone, as the wave names; src/tests/execution-events.test.ts:56-69 — The assertion proves the recorder captured one more face than the single-roll transition and that the first two faces are the scripted 7.
- 2026-09-18, Wave 3.2, opus, cf9e61b 0febe9f, gate pass, APPROVE, no escalation. approved. Reserved: Event types and log tag names (reserved, open from Wave 3.1): this wave adds the question of whether `checkResolved` should carry its target. Provisional answer taken and marked `// proto:` in src/app/presentation.ts — it does not, and the board infers an attack's target from the wounds/disorder the acting unit caused. Appended to the open 'Event types and log tag names' item in docs/plans/service-architecture.todos.md.; checkResolved carries no target: attack and rally markers are inferred from the pieces the acting unit wounded or disordered in the same commit; a miss leaves no mark and a round-ending commit marks nothing (presentation.ts:57-61, todos 2026-09-18 Wave 3.2, appended to the Wave 3.1 event-list entry under 'Open, for Mark'); spellResolved.targets holds units caught rather than shape hexes, so an empty blast plays no burst and Guard/Steady draw no self-marker (todos 2026-09-18 Wave 3.2); Movement routes come from unitMoved events rather than the drag-traced path; an advance's legs are joined into one road (todos 2026-09-18 Wave 3.2). Reviewer notes: src/app/presentation.ts:34-43 — Move and advance routes are unchanged in practice: the drag's `row.path` was built from the same `movePath` and `MeleePlan` legs the service now emits as `unitMoved`, and `routesOf` joins the advance legs; src/app/presentation.ts:57-68 — Markers for ordinary attacks and rallies are inferred from `woundsChanged`/`disorderChanged` on pieces other than `previous.battle.active`, because `checkResolved` names no target; src/app/presentation.ts:128 — `sink.resolved` is called only when a play carries markers or arrows, so a commit with none leaves the previous afterglow to run out its own 800 ms timer; src/app/game.svelte.ts:49-52 — `createPresentation` holds a reference to the last adopted `BattleSession` for the revision comparison and never mutates it; it is seeded from `runtime.session` and updated on every published record, including jumps, so the next single-step commit after a load plays correctly.
